# v12.2 ひっさつわざを わざごとの モーションに ＋ 演出を よける（案・ユーザー決定「①＋②の セット」2026-09-11）

ユーザー「必殺技の エフェクトも バトルを 盛り上げるように、バトルの 画面（アクション）を 邪魔に ならないように、
技によって モーションを 違うように してください。提案してほしい」→ 3案 →「クリア後に ①＋②の セット お願いします」。
（③相棒も わざに 合わせる は 今回 やらない）

## 0. いまの 状態（数えた）

- ひっさつわざ **8しゅるい**：`js/ui/battle.js` の `SPECIALS`（bolt 8／star 12／nova 16／starburst 20）＋`ELEMENTS`（fire／leaf／ice／wind＝5コンボ・教科）。
- **3D の 主人公の 動きは 8つ ぜんぶ 同じ**：`mo-attack`（走って 1回 斬る・0.42秒・`mo-dash`）。てきも ぜんぶ `mo-hurt`。
  場所は `battle.js` の こうげきの ところ（`MQ.ui.v3.dashTo(d.hero, d.cur)` → `play(d.hero, 'mo-attack', 480)` → 320ms 後に `play(foe, 'mo-hurt', 520)`）。
- 派手さは 2D の 演出：`playSpecial(sp)`＝`buildFx`（てきの まわり）＋`playScreenFx`（画面ぜんたい `.fxscreen`：色の 光・集中線・大きな わ・技名の 帯 `.fxband`）＋`cutIn`（0.62秒）＋`blast`（ふっとび）＋`quake`（ゆれ）＋`MQ.sfx.special`。
- **邪魔の 正体**：技名の 帯（`.fxscreen--t3〜t5 .fxband { top: 26px; height: 58px }`・`css/style.css` 1600行あたり）と 画面ぜんたいの 色の 光が、
  ちょうど 主人公の 頭と てきの 上半身の 高さに かぶる → 斬る・やられる 瞬間が かくれる。カットインも 0.62秒 アリーナを おおう。

## ① わざごとの 主人公の 動き（3D・部品を 回すだけ・絵は さわらない）

| コンボ | わざ | 主人公の 動き | てきの やられ方 |
|---|---|---|---|
| 5 | ほのお ギリ（算数 fire） | とび上がって 落ちながら たてに 一閃（ジャンプ斬り） | のけぞって 地面に しりもち |
| 5 | はっぱ カッター（国語 leaf） | 体を ひねって 左右に 2連斬り | 左右に 2回 ゆれる |
| 5 | こおりの やいば（理社 ice） | けんを 前に かまえて 突き ぬける | 固まって（動きが 止まる） かたむく |
| 5 | かぜの たつまき（英語 wind） | 空中で けんを ぐるぐる 回す（手首の 回転） | くるっと 回されて ふらつく |
| 8 | いなずま おとし（bolt） | 高く ジャンプ → てきの 真上から 落下斬り。着地で 地面が ゆれる | ビリビリ ふるえる |
| 12 | ひかりの メテオ（star） | しゃがんで ためる → けんを 天に かかげる。落ちて くる 間は 見上げた まま | いん石に おされて ぺしゃんと つぶれる |
| 16 | ぎんがの ビッグバン（nova） | 空中に うかんで 両うでを 広げる → 光が すいこまれる → 落下と 同時に 爆発 | すいこまれて → 大きく ふっとぶ |
| 20 | スターバースト ストライク（starburst） | 高速の 連続 突き ×6（0.1秒ごと）→ さいごに 大きく 一閃 | 突かれる たびに はねる → さいごに ばたん |

**きまり**
- **3D の 体・頭・あしには 背中の 面が ない**（v12.1 で 軽く した ぶん）。体ごと 1回転（rotateY 360）は 作れない。
  回すのは **けん（`p--sword`・背中あり）と 手首だけ**。うでは 170° まで OK（背中あり）。
- 作り方：`css/motion3d.css` に `mo-sp-<id>`（主人公）と `mo-hit-<id>`（てき）を 足し、`battle.js` の こうげきの ところで
  `sp ? 'mo-sp-' + sp.id : 'mo-attack'`、てきは `sp ? 'mo-hit-' + sp.id : 'mo-hurt'`。長さは `sp.ms` に そろえる
  （`play(el, mo, ms)` の 待機もどしは token）。ダッシュ（`mo-dash`＝器を 走らせる）は わざごとに 使う／使わない を 決める
  （メテオ・ビッグバンは その場、いなずまは ジャンプ、スターバーストは ダッシュ）。
- 主人公の 器 `.v3scene` は 84px。ジャンプは `translateY` で 器の 外に 出て よい（`overflow` なし）。
- 音（`MQ.sfx.special`）と 2D の 絵（`buildFx`）は そのまま。**ゆれ・ふっとび・技名・音は へらさない**（息子さん「もっと 派手に」）。

## ② 演出を「主人公と てきが 見える 場所」に よける

- 技名の 帯 `.fxband` を **アリーナの いちばん 下**（問題カードの すぐ 上）へ。主人公と てきに かぶらない。
- 画面ぜんたいの 色の 光（`.fxscreen` の tint）は **アリーナの 中だけ**（問題・テンキーは 光らせない・読める まま）。
- 集中線と 大きな わの 中心を **てきの 位置**に（いまは 画面の まん中・`top 112`）。てきの 中心は `d.cur.getBoundingClientRect()` を stage の scale で 割って 出す。
- カットイン（`cutIn`）は **12コンボ 以上（tier ≥ 3）だけ**。5〜8コンボは 動きで 見せる。合体（相棒と いっしょ）は いままで どおり。
- 演出の 長さ（`sp.ms`）・ゆれ・音は いまの まま。

## 3. たしかめ方

- harness `#fx:<わざ>[:<秒>]`（fire leaf ice wind bolt star nova starburst）＝止めて 撮る。3D の 動きは `getAnimations()` で 止める（`#demo3dbattle:attack:0.3` と 同じ 型）。
  **予約ずみの setTimeout は freeze() で 止まらない**（番号で clearTimeout）。
- 520×940・800×1280・520×780 で「主人公と てきが 帯・光に かくれて いない」を 目で 見る。
- `#perf3d`（面の 数は ふえない はず・動きだけ）。smoke ALL OK ×3・`#flow` エラー 0・`#fit:all` 2サイズ。
- sw は **127**・news.js に v12.2。

## 4. やらない こと

- ③相棒も わざに 合わせる（5コンボ：うしろから いっしょに／12以上：はさみうち）→ ユーザーが えらばなかった。あとで 聞く。

---

# できあがり（2026-09-11・sw 127）

## 入れた もの

**① わざごとの 3D の 動き**（`css/motion3d.css` の さいご「v12.2」の 節・`js/ui/battle.js` の `SP_MOTION`／`specialMotion`・`js/ui/three.js` の `play(el, mo, ms, opts)`）

| わざ | 主人公 `mo-sp-<id>` | 器（`.v3scene`） | てき `mo-hit-<id>`（当たる 時間＝CSS の delay） | ザコが たおれ始める |
|---|---|---|---|---|
| fire ほのお ギリ（.95s） | 走って → とび上がり（-42px）→ 落ちながら ふり下ろす（55%） | `mo-dash-sp` | のけぞって しりもち（.5s〜） | 950ms |
| leaf はっぱ カッター（.95s） | 走って → 体を ひねり（rotateY ±22°）右うでを 左右に 2回 | `mo-dash-sp` | 左右に 2回 ゆれる（.34s〜） | 800 |
| ice こおりの やいば（1s） | てきの 手まえで ため → けんを 右に かまえて てきの むこう（+24px）まで 走りぬける | `mo-dash-thru` | こおって 止まり（あし・はねも 止める）→ かたむく（.36s〜） | 820 |
| wind かぜの たつまき（1s） | 走って → 空中（-34px）で うでを 右に のばし けんを 3回転（手首 rotateZ） | `mo-dash-sp` | 右へ くるっと 回されて（rotateY 58°）ふらつく（.38s〜） | 850 |
| bolt いなずま おとし（1.1s） | しゃがみ → 高く ジャンプ（-60px）→ てきの 真上から ふり下ろす（55%＝着地） | `mo-dash-jump` | ビリビリ ふるえる（.6s〜） | 1000 |
| star ひかりの メテオ（1.35s） | その場で しゃがんで ため → けんを 天に（176°）→ 見上げた まま | なし | ぺしゃんと つぶれる（scale 1.28, .55）（.62s〜） | 1000 |
| nova ぎんがの ビッグバン（1.9s） | 空中（-52px）に うかんで 両うでを 広げる → 30%（0.57秒）で 落ちて ふり下ろす | `mo-rise` | 小さく すいこまれ → 大きく ふっとぶ（.55s〜） | 1150 |
| starburst スターバースト ストライク（2.1s） | 走って → 0.1秒ごとに 6回 突く（24〜52%）→ ふりかぶって 一閃（62%＝1.3秒） | `mo-dash-sp` | 突かれる たびに はねる ×6 → さいごに 大きく のけぞる（.5s〜） | 1600 |

- **体は 回転させて いない**（背中の 面が ない）。回すのは けん（`p--sword`）と 手首だけ。ひねりは rotateY ±22°まで（器の 22°と 足して 0〜44°）。
- **てきの やられ方は 当たる 前から クラスを つけて、CSS の `animation-delay` で 当たる 時間に 始まる**。setTimeout だと harness の `freeze()` で 止まって 撮れない。
  それまで てきは 待機（`mo-menace`）が 外れて 立ち止まる＝「くる…！」に 見える ので そのまま。
- ザコが たおれ始める 時間（`SP_MOTION.down`）は **3D の ときだけ** 当たって から 0.45秒 あと（やられ方を 見せる）。2D は いままで どおり（420／600／750）。
- **ジャンプの 高さは 60px まで**。アリーナが いちばん 低い 210px（stage 700）でも 主人公の 上の あきは 64px（=210-62-84）。それ以上 とぶと 頭が 切れる（`.arena` は overflow hidden）。
- 器の 走り方の 長さは `--dms`（JS が `sp.ms` を 入れる）。`animation-duration: var(--dms)` は Chrome／Safari で 効く。
- **2D の「大きく とびこむ」（`.hero.is-special`＝36px 右へ 1.3倍）は 3D では 止めた**（器の 走りと 二重に なる）。オーラ（`::before`）は のこす。
- `play()` の 器の クラスは `clearSceneMo()` で ぜんぶ 外して から つける（`mo-dash` だけ 外す 書き方だと `mo-rise` が のこる）。

**② 演出を よける**（`css/style.css`・`js/ui/battle.js` の `playScreenFx`／`foeCenter`／`playSpecial`）

- 技名の 帯 `.fxband`・`.fxname`・`.fxstars` は **アリーナの いちばん 下**（`top: calc(var(--arena-h) - 54px)`・t3〜5 は -62px）。`--arena-h` は `playScreenFx` が `d.arena.offsetHeight` を 入れる。
  アイテムの わざ名（`.fx .fxname`＝アリーナの 中）は いままで どおり 上（top 34）。
- 色の 光 `.fxscreen__tint` と まっくら `.fxscreen__dark` は **高さ `--arena-h`＝アリーナの 中だけ**。問題カード・テンキーは 読める まま。
- 集中線 `.fxscreen__lines`・わ `.fxscreen__ring`・光の 中心は **てきの 中心 `--fs-x`／`--fs-y`**（`foeCenter()`＝`.enemy__img(3d)` の getBoundingClientRect を `.battle` の 左上から ステージの 拡大で 割った px。てきが いなければ 348, 112）。
- **カットインは tier ≥ 3（12コンボ〜）か 相棒と いっしょの ときだけ**。場所は **アリーナの 上**（`.cutin` top 84 → 8）＝帯が 下に 来た ので まん中だと かさなる。
- 長い 技名は 400px から はみ出して いた（前から）→ `ぎんがの ビッグバン！` 30px・`スターバースト ストライク！` 26px。
- ゆれ・音・長さ・ふっとび・敵の まわりの 絵（`buildFx`）は 1つも 変えて いない。

## たしかめ方

- harness **`#fx:<わざ>:<秒>`**＝ログに `fx3d: hero=mo-sp-<わざ> scene=mo-dash-* dash= dms= foe=mo-hit-<わざ>` と `fxband: y a〜b / arena h`（帯の 下 ≤ アリーナの 高さ）。
  撮る 時間の 目安：fire .45／leaf .34／ice .4／wind .25 と .5／bolt .42（空中）と .55（着地）／star .6／nova .3（うかぶ）.7（落ちた あと）／starburst 1.3（一閃）。
- harness **`#spatk:<わざ>[:<秒>]`**＝本物の 道（answer → attack）。コンボを ためて ひっさつを 出し、当たった あと `foe=mo-fall is-down=true`、つぎの てきで `hero=mo-idle scene=なし` を 見る。
  **nova／starburst は 16／20コンボ より 先に ステージが おわる**（ボスまで たおす）ので `#fx` で 見る。
- 撮った：520×940・800×1280（タブレット＝アリーナ 210）・520×780。3サイズとも 帯は アリーナの 下・主人公と てきは 見える。
- smoke ALL OK ×3・`#flow` エラー 0。`#fit:all` は 回して いない（さわったのは 絶対配置の 演出だけ・カードの 高さは 変えて いない）。
- **実機 未確認。**

## つぎに 聞く こと

- 息子さんに：わざの 動きが わかるか（とくに こおり＝走りぬけ・かぜ＝けんの 回転）／帯が 下に 来て 見やすく なったか／5〜8コンボの カットインが なくて さびしく ないか。
- ③相棒も わざに 合わせる（うしろから いっしょに／はさみうち）は やって いない（ユーザーが えらばなかった）。

---

# ③ 相棒も わざに 合わせる（v12.3・sw 128・2026-09-11）

ユーザー「確認して動作確認OK!③お願いします」（v12.2 を 実機で 見た あと）。**ルールは 変えない・見た目だけ**（追い打ちの 回数・けいけんちは そのまま）。

| コンボ | 相棒の 動き | 器の クラス（`.pal .v3scene`） |
|---|---|---|
| 5〜11（tier 1・2） | 主人公の うしろを ついて 走り（主人公の 走った 先の 48px 左）、当たる ころに 手まえへ とび出す | `mo-pal-follow`（1秒・35% で つく・70% まで いて もどる） |
| 12〜（tier 3〜5） | てきの 右がわ（右はしより 22px 左）に 山なりに とんで 着地 → はさみうちで とび出す | `mo-pal-flank`（1.2秒・48% で 着地） |

- 体は `mo-pal-sp`（走る あいだ はね・あしを ばたばた・`--phit` の 時間に モンスターの こうげきと 同じ `moPounce`）。
- 数字は `battle.js` の `palJoin(sp, foe, m)` が 入れる：`--pdash`（行き先 px）・`--pdelay`（当たる 時間 − 500／600ms。マイナスも OK＝途中から）・`--phit`（当たる 時間 − 200ms）。
  当たる 時間は `SP_MOTION.palHit || hit`（**スターバーストだけ 1300＝さいごの 一閃**）。
- `three.js` の `play()` は `opts.scene` が あれば 相棒（主人公 いがい）にも 器の クラスを つける。
- **追い打ち（`palAttack`）と 重なった とき（合体）**：③で 走って いる あいだ（`palJoinUntil`）は 2D の `is-hit` ジャンプと `mo-attack` を 出さない（二重に 動く）。帯・音・斬撃は そのまま。
- **カットイン中に 相棒を 消す（v10.5）を やめた**（`.arena.is-cutin .pal`）。カットインは v12.2 で アリーナの 上に うつり、相棒は 下の 草に いて かさならない。消したままだと ③で 走って いる 相棒が 消える。ふきだしは いままで どおり 消す。
- 相棒が いない 子には 何も 変わらない。2D（りったい オフ）は いままで どおり（2D の 追い打ちだけ）。
- たしかめ：harness **`#fxpal:<わざ>:<秒>`**（ログ `fxpal: pal=mo-pal-sp scene=mo-pal-follow/flank pdash= pdelay= phit=`）・`#palfx`（合体＝器 follow・体 mo-pal-sp・2D の is-hit なし）。
  撮った：fire 0.3（相棒が 主人公の うしろを 走って いる）／star 0.3（右へ とぶ とちゅう）／800×1280・520×780。smoke ×3・`#flow` エラー 0。**実機 未確認。**
