# v13.3 地図の 下を アイコンの ドックに（2026-09-12・sw 140）

## きっかけ
ユーザー「ごちゃまぜバトルと 修行場の 位置や デザイン・アイコンの 壁打ちしましょう」→ 3案を 本物の 地図に かさねた 見本
（https://claude.ai/code/artifact/7b969b18-f62c-45fd-b30a-e694c857493b ・デスクトップ `地図のボタン案/`）→「**Aの案でお願い。今すぐ作成して。**」
アイコンは 見本で おすすめした **サイコロ（ごちゃまぜ）と まきもの（しゅぎょうば）**。

## まず 数えた（タブレット 800×1280＝ステージ 700）
| | 地図の 高さ |
|---|---|
| v8.0 で 広げた とき | 474 |
| v13.1（しゅぎょうば＋ごちゃまぜの 帯） | 423 |
| てがみ・にげた敵も ある 日（帯 4本） | **311**（画面の 44%） |
| **v13.3 ドック** | **513（どの 日も）** |

ほかに：ごちゃまぜの アイコン（赤・みどり・水色・黄の 2×2）が Windows の マークに にて いた（ほかの 家庭に 配る ので 変えた）。
しゅぎょうばの アイコン（相棒の 顔 30px）は「教えて もらう 場所」に 見えなかった。

## できた もの
- `js/ui/map.js`：`DOCK_ICONS`（dice／scroll／hourglass／book・ブロックの ぬり方）と `dockIcon()`（`MQ.ui.map.dockIcon`）。
  下の 部分を ドック（メニュー／しゅぎょう／ごちゃまぜ／タイム／学年）＋ 地図の すみの ふだ（てがみ・にげた敵）に。
  プレイヤーを かえる ボタンは 学年えらびの 中。地図の いちばん 下に あき（`.map__pad`）。できる ことが ふえた（v11.1）の アイコンも サイコロ。
- `css/style.css`：古い 帯（`.mixbtn` `.dojobtn` `.tegamibtn` `.revenge` の バー）の CSS を 消して、いちばん 下に「地図の ドック（v13.3）」の 節。
- `js/content/news.js` v13.3（kind `dock`＝`js/ui/news.js` が ドックの アイコンを 出す）。smoke の KINDS に `dock`。
- harness `#dock` `#dock:busy` `#dock:grade`。

## もどさない
- 帯を つみ上げる 形に もどさない（混む 日に 地図が 半分 いかに なる）。
- ごちゃまぜは **まん中の いちばん 目立つ ボタン**（v7.3 の にがて対策の 1つ。目立たなく しない）。
- 4まいの 四角の アイコンに もどさない。
- **ふだは ごちゃまぜの コインの ふだより 上**（`.mapchips` top −66px）。−48px だと にげた敵の ふだが コインの ふだに ふれた。
- class 名は harness が 見る ので 変えない（新しい 見た目は CSS で 上書き）。

## たしかめた こと
- smoke ALL OK ×3・`#flow` エラー 0。
- 800×1280・520×780 で `#dock` `#dock:busy` `#dock:grade` `#firstplay:map` `#dojomap` を 撮って 見た。
- **実機 未確認。**
