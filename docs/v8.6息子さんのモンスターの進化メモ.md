# v8.6 息子さんの モンスターに 専用の 進化形（2026-09-06）

ユーザーの ことば：**「息子さんの 4体（スカルホース・サメオニ・ずかんの あくま・ABC兄弟）に
専用の 進化系を作成して。」**

v8.2 で もとから いる 88体は 44の 系統に なり、写真から 作った じぶんの モンスターも
3段階に なった。でも **息子さんが 絵で 作った 4体だけが 進化しない まま**だった
（v8.2 の のこり課題として CLAUDE.md にも 書いて あった もの）。ここを うめた。

---

## 1. 何が できたか

**6系統 × 3段階 ＝ 進化形 12体を 新しく 作った。** 図かんは **208 → 220体**。

| 1段階め（いままで） | 2段階め（Lv.10） | 3段階め（Lv.20） | エリア |
|---|---|---|---|
| スカルホース | **ブレイズホース** | **スカルロード** | 算数の山 |
| サメオニ | **キバサメオニ** | **サメオニノヌシ** | 理科社会の海 |
| ずかんの あくま | **やみの ずかん** | **ずかんの だいまじん** | 国語の森 |
| エー | **エーナイト** | **エーロード** | 英語の空 |
| ビー | **ビーナイト** | **ビーロード** | 英語の空 |
| シー | **シーナイト** | **シーロード** | 英語の空 |

追い打ちの つよさは v8.2 の きまりの まま（`MQ.pals.POWER`）：
1段階 EXP+10 / 2段階 EXP+15 / **3段階 EXP+20・ボスに 2ダメージ**。

---

## 2. 決めた こと（もどさない）

### ① もとの 絵の かたちを 変えない
息子さんの 絵が 分からなく なると **v2.4.1 の 苦情「自分の作った モンスターの
再現性が 低い」に もどる**。だから 足すのは **かざりだけ**（炎・つの・かんむり・
マント・ぶき・オーラ）。サメオニの **目 3つ**、スカルホースの **馬＋ガイコツの きし**、
ずかんの **羽つきの 本**、ABC の **字の かたち** は 3段階とも そのまま。

### ② ABC は 1文字ずつ べつの 系統
3つが 1体に 合体する 案は **やめた**。エーと ビーの 2体を なかまに して いる 子が
両方を 育てると、`p.pals[つぎのid]` が 上書きされて **片方の 相棒（けいけんち・つけた
なまえ）が 消える**。だから `abc-a` / `abc-b` / `abc-c` の 3系統に 分けて ある。

### ③ 2・3段階めは ふつうの たたかいに 出ない（`evoOnly: true`）
出会うのは いままで どおり 1段階めだけ。写真から 作った モンスター（v8.2）と 同じ
きまり。**`rareIdsFor()` の 息子さんぶんにも `!e.evoOnly` を 足した**
（v8.2 では customs にしか 入って いなかった）。

### ④ 進化形に `trio` を つけない
`trioFor()` は「その エリアの trio を ぜんぶ」返す。進化形にも つけると
ABC3きょうだいが 9体に なって バトルが こわれる。smoke が 3体で ある ことを 見る。

### ⑤ 名前に「キング」「まおう」を つかわない
ボス（キングスライム）・ラスボス（まおう）と かぶる。v8.2 の 王さま 44体と 同じ きまり。

### ⑥ id は `-2` / `-3`
smoke の v4.2「51体」検査は `e.line && /-[123]$/.test(e.id)` で 数えて いる ので、
**`!e.rare` を 足して** 息子さんの ぶんを よけて ある（息子さんの 4体は ぜんぶ `rare: true`）。

---

## 3. 作り方（手で 座標を 打たない）

道具は **`tools/son/`**。v8.2 の `tools/king/` と 同じ 3段がまえ。

```
node tools/son/build.js    # 絵を 組み立てて tools/son/sheet.html と arts.json を 書く（まだ 入れない）
node tools/son/apply.js    # monsterart.js と enemies.js に 入れる（2回 流しても こわれない）
```

| ファイル | 中身 |
|---|---|
| `tools/son/defs.js` | **どう かざるか の 決めごと**（1体ずつ）。`base`（もとの 絵）／`fitTop`（上に すきまを 作る）／`back`（うしろの 四角）／`front`（前の 四角）／`crownBox`＋`crown`／`spec`（king の 部品） |
| `tools/son/build.js` | 組み立て＋検査＋一覧ページ。**`tools/king/kit.js` の 部品（CROWNS/BACKS/HANDS/EXTRAS）を そのまま 借りる** |
| `tools/son/apply.js` | ゲームに 入れる |

### `fitTop`（v8.6 で 足した しくみ）
スカルホース・サメオニは **すでに 48マス いっぱい**で 上に すきまが ない。
- 字（A・B・C）は `fitTop` で **絵を すこし 小さく して 上を あける** → かんむりを のせる。
- スカルホース は 小さく すると 細かい ところが つぶれる ので、**かんむりを 馬の 頭の 上**に
  置いた（`crownBox: { hcx: 8, top: 13, hw: 14 }`）。
- サメオニ は **つの 2本の あいだ**に 置いた（`crownBox: { hcx: 24, top: 9, hw: 18 }`）。
  band は y6〜10、まん中の 目は y10 から なので **目 3つは かくれない**（実測ずみ）。

### 「かくれる かざり」の 検査（大事）
`build.js` の `hiddenReport()` が、**うしろに 置いた 四角が 7わり いじょう 本体に
かくれて いたら 名ざしで 出す**。1回目は マントが 97% かくれて いた（本体の うしろに
入れても 見えない）。**かざりは 本体の 外がわに 出す**のが 正解。
- スカルホースの マント → きしの **うしろ（右がわ）**に ながす
- サメオニの マント → やめて **大きな ひれ**に した
- 字の マント → やめて **光の わ（aura）／はね** に した（マントは 字を かくす）

### 白い 四角に 見えない ように
きば・トゲは **1つの 四角だと ただの 白い はこ**に 見える。**2つ かさねて 先ぼそり**に する。

---

## 4. 見る・たしかめる

```
node tools/smoke.js .                       # 図かん 220体・6系統の ならび・レア敵に 出ない・Lv10/Lv20
tools/harness.html#sonevo                   # 上の ことを ログで 見る
tools/harness.html#sonevo:art               # 18体を ならべて 見る（撮る 用）
tools/facelab.html#zoom:sameoni-3,zukan-3   # 大きく して 見る
tools/son/sheet.html                        # 1段階 → 2段階 → 3段階 を 96px と 48px で ならべる
```

`#flow` エラー 0・smoke ALL OK（3回）で 確認ずみ。**実機は 未確認。**

---

## 5. お知らせ画面（v8.3）にも 1行

`js/content/news.js` に `v8.6`（sw 101）を 足した。
絵は スカルホースの 3だんかい（`grow: true`）。文は **62字まで**（smoke が 見る）。

---

## 6. 息子さんに 聞く こと

- **名前は これで いい？**（スカルロード・サメオニノヌシ・ずかんの だいまじん・
  エーロード…）。気に入らなければ **図かん →「なかま」→「なまえを つける」**で
  自分で つけられる（8文字・v5.2）。進化しても つけた なまえは のこる。
- 2段階め（Lv.10）が **1段階めと ちがうと すぐ 分かる？**
- 3段階めは **かっこいい？** それとも かざりが 多すぎる？
- ABC は **1文字ずつ 育つ**のと **3体が 合体する**のと、どっちが よかった？
  （いまは 安全のため 1文字ずつ。合体に すると 2体 育てて いた ときに 1体 消える）

## 7. のこって いる こと

- 息子さんが **写真から 4体を 作り直す**と、そちらは `my-…` の 自動生成の 3段階に なる
  （v8.2 の `monstergen.evoParts`）。**この 12体は 手で 作った 専用の すがた**なので、
  作り直しても もとの 4体（`skullhorse` など）の 進化形は のこる。
- 実機で たしかめる（v2.6 いこう ぜんぶ 未確認）。

---

# v8.9 なかまの ねだんを 段階べつに（2026-09-06）

進化を ぜんぶに 広げた あと（v8.2・v8.6）、**コインの お店の 穴**が 見つかった。
見つけたのは カプセルマシン（v8.8）を 作って いる セッションの 実測。

## 何が まずかったか
`price()` は **つよさ（rank）べつ**の `3 / 6 / 10` だった。王さま形は rank3 なので
**Lv.20 まで 育てて やっと なる 姿が コイン 10まいで 買えた**。
自力なら けいけんち **5890**（Lv.20）かかる ので、**育てるより 買う ほうが 早い**。
これでは 進化が ごほうびに ならない。

## 直した こと（ユーザー決定：案A）
`js/core/pals.js`

```js
const PRICE      = { 1: 3, 2: 20, 3: 40 };   // 段階べつ（育てた ほうが 早い）
const PRICE_SOLO = { 1: 3, 2: 6,  3: 10 };   // 系統に 入って いない もの
function price(id) {
  const e = enemyOf(id);
  if (!e) return 0;
  if (e.stage) return PRICE[e.stage] || 3;
  if (e.by === 'photo') return 3;             // むかしの セーブ（段階なし）
  return PRICE_SOLO[e.rank || 2] || 6;
}
```

**お店を 消さない のが 大事**（もう 1つの 案「1段階めしか 売らない」は 採らなかった）。
実測：3まい **71体**／20まい 63体／40まい 67体／6・10まい 9体。
お店は ねだんの 安い 順に ならぶ ので、**まず 3まいの 子が ならぶ**（すかすかに ならない）。

## もどさない こと
- **3段階めは 40まい いじょう。** ここを 下げると また「買う ほうが 早い」に もどる。
- **ゴールデンスライムと 中ボスは いままで どおり**（系統に 入って いないので `stage` が ない）。
- smoke が 見る：1/2/3段階 = 3/20/40・息子さんの ぶんも 同じ・系統なしは 3/6/10・
  **3段階めは ぜんぶ 40まい いじょう**・**3まいで 買える 子が 50体 いじょう**。

## のこって いる こと（別セッション v8.8 が やる）
`shopList` は **図かんに のって いれば 何でも 売る**。
**`evoOnly`（12体）と `mid`（8体）は お店から 外す**べき
（`evoOnly` は 育てないと 手に 入らない もの）。ねだんの 変更とは 別の 直し。
