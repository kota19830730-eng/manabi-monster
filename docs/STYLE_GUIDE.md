

## りったい（3D・v12.0）

主人公・モンスター・たからばこ・相棒は **バトル・タイトル・カプセルマシン**で 箱の 立体に なる（ずかん・地図・けっか画面・HUD は 2D の まま）。
絵は 2D の ドット絵 そのまま（描き直さない）。仕様は `docs/v12.0りったいメモ.md`・作り方の きろくは `docs/3Dの試作メモ.md`。

- **器**：`.v3scene`（perspective 900px）> `.v3fig`（`rotateX(8deg) rotateY(±22deg)`＝左の 子は +22・右の 子は −22）> `.v3hold`（`scale3d`・下の まん中が じく）> `.v3`（部品 `.p--*`）。
- **光**：左上の 手まえ。上の 面＝あたたかく 明るく、右・下＝すこし 青い かげ。うしろほど 暗く（AO）。ざらつき ±6%。
- **厚み**：ぜんたい＝かたちの 短い 辺 × 0.42（上限 18マス）。主人公は 帯（頭 20／体・足 13）。けんの 刃は 4。たからばこは 24。
- **動き**（`css/motion3d.css` の `mo-*`・かんせつで 回すだけ・曲げない）：待機 `mo-idle`（主人公）`mo-menace`（てき＝前かがみで ゆすり 3.4秒に 1回 フェイント）`mo-title`（タイトル・相棒）`mo-chest`（ふたが ガタガタ）／
  登場 `mo-walk`（1体め・ボス）`mo-hop`／こうげき `mo-attack`＋器の `mo-dash`（0.42秒・てきの 手まえまで）／くらう `mo-hurt`／たおれる `mo-fall`（横に ばたん）`mo-crumble`（ボス）／かち `mo-win`／ため `mo-lunge`／たからばこ `mo-chest-open`。
- **2D の 器の 動きは `.is-3d` で 止める**（`.hero.is-attack`・`.enemy.is-appear`・`.is-lunge`・`.is-struck`・`.is-counter`・`.is-down`・`.is-bossdown`）。のこす もの：ひっさつの ふっとび・よける・ガード・にげる・ぶんしん・斬撃の 弧・つぶ・白フラッシュ。
- **3D の 図に `filter` を かけない**（平らに なる）。2D の 影（`.shadow--hero/--foe`）は かくして 足もとの だ円に。
- せってい「りったい」（はじめは つける）で 2D に もどる。**2D の 道は 消さない**。
- **v12.1 軽く**（`docs/v12.1軽くするメモ.md`）：見えない 面を 作らない（かくれる 面・向きで 見えない がわ・うしろは うで／けん／ふた だけ・
  タイトルと カプセルは 上の 面も なし）＋ 同じ 平面の 面を つなぐ ＋ 模様は 1体 1まいの アトラス ＋ cloneNode。
  **面の 置き方（内むき・両面）は v12.0 の まま**（外むき＋backface-visibility は あごの 下に すきまが 出た）。
  前の 面は 四方に 0.5px 大きく・右と 下の 面は 0.6px 手まえ（つぎめに 空が すけない）。
- **2D の タイトルの 勇者も 3D と 同じ 見本キャラ**（`TITLE_HERO`・`.title__heroimg--avatar` 138px）。v5.0 の 一枚絵は つかわない。

## バトルの 相棒の 場所（v12.1）

相棒は **主人公の うしろ（左はし）・同じ 草の 上**（`.pal` left 6 / bottom 62 / z-index 1）。なまえと ゲージは 足もとの 下の 暗い ゆかに。
そのために 主人公を 32px 右へ（`.hero` left 46）、ふきだしは left 132 / max-width 152（右はしは 284 の まま）、
ためゲージと コンボは left 54。**主人公を 動かさずに 相棒を 左はしに おくと 主人公の かげに かくれる**。
v10.5 の「まん中・暗い ゆか（left 190 / bottom 12）」には もどさない（相棒だけ 地面が ちがい、主人公の ダッシュが 相棒の 上を 通って いた）。


## ひっさつわざの 動きと 演出の 場所（v12.2）

仕様の 正本は `docs/v12.2わざモーション案.md`（表と できあがり）。
- **8わざ ぜんぶ ちがう 3D の 動き**（`css/motion3d.css` の `mo-sp-<id>`／`mo-hit-<id>`・器の `mo-dash-sp`／`mo-dash-thru`／`mo-dash-jump`／`mo-rise`）。
  体は 回転させない（背中の 面が ない）。回すのは けんと 手首。**ジャンプは 60px まで**（アリーナ 210px でも 頭が 切れない）。
- **技名の 帯は アリーナの いちばん 下**（`--arena-h` − 54px・大わざは − 62px）。アイテムの わざ名だけ 上の まま。
- **色の 光と まっくらは アリーナの 中だけ**。集中線・わの 中心は **てきの 位置**（`--fs-x`／`--fs-y`）。
- **カットインは 12コンボ〜（tier ≥ 3）と 相棒と いっしょの ときだけ・アリーナの 上（top 8）**。
- 長い 技名：ぎんがの ビッグバン！ 30px／スターバースト ストライク！ 26px（400px に おさめる）。
- ゆれ・音・ふっとび・敵の まわりの 絵は v2.5〜v7.5 の まま（へらさない）。
- **③ 相棒も わざに 合わせる（v12.3）**：5〜11コンボ＝主人公の うしろを ついて 走って とび出す（`mo-pal-follow`）／12コンボ〜＝てきの 右がわに とんで はさみうち（`mo-pal-flank`）。体は `mo-pal-sp`。
  数字は `battle.js` の `palJoin()`（`--pdash`／`--pdelay`／`--phit`）。**カットイン中に 相棒は 消さない**（v10.5 の きまりは v12.2 で カットインが 上に うつった ので 不要）。合体の ときは 追い打ちの 2D ジャンプを 出さない。

## りったいを 軽く（v12.5）

- **よこ・上下の 面は 色だけ**（`vox.js` の `sideFill`）：1色なら `background-color`、行ごとに 色が ちがえば 硬い しまの `linear-gradient`。明るさは 面の 光 × 0.92。
  **画像（アトラス）を よこ・上下の 面に もどさない**（画像を はった 層が 重さの 正体・`docs/v12.5軽くするメモ2.md`）。前の 面と 主人公の 絵は 画像の まま。
- 1ばいの 見た目は 同じ（4ばいで よこの ざらつきが 平らに なるだけ）。合格は `#perf3d` の 上位5% が 20ms いか。
- **タイトルの 地面の 下は 茶**（v12.5.1・ユーザー「ブルーの 下の カラーが 気に なる。ブラウンに 統一」）：`.title__land::after` で ボタンの 台と 同じ 茶の しま（#7d502c／#714727）を 地面の 下に 600px のばす。オープニングで ボタンが せり上がる まえも 空の 青が 見えない。

## 背景（v12.6）

タイトル・バトル・地図の うしろに **遠景と ゆか**。A（遠くの 山なみと まおうの 城）＋B（ブロックの 森）＋C（夕やけ）を ぜんぶ 入れた。中身は `js/ui/scenery.js`、色と 置き場所は `css/scenery.css`。仕様は `docs/v12.6背景メモ.md`。

- **画像なし・filter なし**。ぜんぶ `<i>` の 四角を `bottom` で ならべる（`.bgfar`）。3面の 光は `.bk`（inset の 白と 黒）。山なみは `range()`＝**6px の 段・12px ごと・同じ 高さは 1つの 四角**。
- **空は 本当の 時計で 変わる**（C）：朝 6〜9／昼 9〜16／夕 16〜19／夜。`.title.tod-*`・`.arena__bg.tod-*` が 空の グラデーションを 切りかえる。**時計で 変わるのは タイトルと バトルの 山・湖・町だけ**。森＝ずっと 夕方・海と 空＝昼・塔＝夜（v1.2 の「エリア別の 時間帯」を のこす）。
- **タイトル**：`.title__scene` の 先頭に `.bgfar`（bottom 0・高さ 150・z 0）。遠くの 雪山 2重（yoff 20）＋松＋城（x 296・旗と 窓の 明かり。ドラゴンの うしろで 旗だけ 見える）＋鳥＋きり／手まえに みどりの 丘 2段＋木 3本＋しげみ＋花。**太陽・月は `.title__top`（かんばんの 上の あき 52px）の まん中**（x 216〜264＝音の ボタンと おうちの人の あいだ）。空の いちばん 短い ときは 126px しか ない（タブレットは いつも これ）ので、scene の 中の ものは **126px に おさめる**。
- **バトル**：`.arena__bg` の 中は 空 → `.arena__dusk` → 雲 → `.bgfar`（bottom 74・高さ 120）→ 草 → `.afloorwrap > .afloor`。**`.arena__hills`／`.arena__ground` は 消した**。エリアごと＝山（雪山・松・小さな 城・きり）／森（木 4本・丘・花）／海（水平線・ヤシ・遠くの 島）／空（雲の 床・白い 城）／塔（石の 柱・むらさきの クリスタル・星）／湖（水面・あし・山）／町（家なみ・夕方と 夜は 窓に 明かり）。**右 120px（てきの 場所）に 高い ものを 置かない**（遠くの 山だけ）。
- **ゆか**（3案 共通）：`perspective(260px) rotateX(52deg)` の 板に `repeating-linear-gradient` の マス目（34×30）。色は エリアごと `--fl1`／`--fl2`（石／土／すな／雲／光る 石だたみ／ぬれた 石／石だたみ）。
- **ボス戦で 空が 暗く なる**：`bossIntro()` が `.arena__bg.is-dusk`（むらさきの 幕 1.6秒・遠景 .55）、おこると `is-dusk2`（赤黒く）。**塔は もともと 夜 なので 効かない**（CSS で 打ち消す）。`paintScene()` が className を 書き直す ので つぎの たたかいで 消える。
- **地図**：島・道・マス目は さわらない。`.mapfar`（いちばん 上 18px の うすい 山なみ・テーマごとの 色）＋`.map__tint`（z 3＝ノードより 下：朝は うすい 金・夕は オレンジ→むらさき・夜は 紺 .3）＋木・家・岩の てっぺんに 光。
- 軽さ：部品は バトル 60〜125こ・タイトル 145〜160こ（smoke が 数える）。**1コマの 時間は 遠景を 消しても 変わらなかった**（PC の ばらつき ±20ms の 中）。
- 撮る：harness `#scenery[:朝|昼|夕|夜]` `#scenery:arena:<biome>[:時間帯]` `#scenery:map[:時間帯]` `#bossdark` `#bossdark:hot`。ふだんの harness は **昼に 固定**（`fresh()` の `setNow`）。

## ボスの 見せ方（v12.7）

- ~~HP の 玉は 足もと~~ → **v12.8 で 1体の ボスは 右上の パネル `.bossinfo`（名前・弱点・ため）＋ 左上の 赤い バー（HP）に なった**（足もとの 玉は 出さない）。玉が 出るのは 2体 ならぶ とき（ぶんしん・呼ばれた ザコの となり）だけ。7こ いじょうの ときは `.bosshp--many`（11px）。
- **上の ピル**：ボスの ときは「にげるまで N」（N＝のこりの 問題数）。
- **本気モード**：むらさきの 輪の オーラ（`.enemy--hard::before`・`bossAura` 2.2秒）＋ 右の かたに むらさきの リボン「本気」（頭の 上は ため・わざの ラベルが 出る ので 使わない）。
- **さいごの 力（第3形態）**：赤い 輪の オーラ（`.is-final::before`・0.9秒で どくどく）。本気＋さいごの 力は 赤と むらさきの 2色。
- オーラは **::before の radial-gradient の 輪**（まん中は ボスの 絵に かくれる ので 輪に しないと 見えない）。3D の 図に filter を かけない。
- **ふつう／本気 を えらぶ パネル**（`.bosspick`）：問題の 場所に かぶせる 紺＋金わく。上＝クリーム色の「ふつうに たたかう」、下＝むらさきの「本気の ボスと たたかう」（光沢つき）。
- まとめ問題の カードの 上の 字は「まとめ：ステージ名」（こい 金色・太字 `.card__unit--recap`）。

## ボス戦の 画面を 見やすく（v12.8）

息子さんの 実機（タブレット・ステージ 700＝いちばん 短い）の 写真「バトルシーンが 台無し」から。ふきだしが 4行（弱点の 文が 2回 ついて いた）・上の ピルが 2行に 落ちて HPバーが 勇者に かぶる・ラスボスの 名前が 頭の 上で 画面から はみ出す・足もとの HP と 弱点が 右下の アイテムボタンに かくれる、の 4つが 重なって いた。

- **1体だけの ボスの 名前・弱点・ための 玉は 右上の パネル `.bossinfo`**（top 6・right 10・2行＝名前／弱点＋ため）。3行に すると ラスボス（112px）の 頭に かぶる。**ボスの HP の 玉は 出さない**（左上の バーが ボスの HP＝`.hpbar--boss` で 赤・ふきだしが「あと Nかい」）。ぶんしん・呼ばれた ザコの となり（twin）は いままで どおり 足もと。
- **ボス戦の 上の ピルは「にげるまで N」だけ**（「ラスボス」「ボス」の ラベルと ×2 は 外す）→ タイムと 1行に おさまる（256px）。
- **ふきだしは 3行まで**：ボス戦は 名前を くり返さない（「おこって いる！ あと 6かい！」「さいごの 力だ！ あと 1かい！」）、弱点は「弱点！ チャンス！」（教科名は パネルに ある）、大わざは「大わざだ！ 正解で カウンター 2ダメージ！」。塔の ふきだしは max-width 136px（ラスボスに かぶらない）。
- **「N コンボ！ ひっさつ！」は 20px の 2行**（`.combo.is-sp`・max-width 176）。26px 1行だと 右下の アイテムボタンの 下に もぐる。
- **相棒の なまえと ゲージは 左はしに そろえる**（まん中ぞろえだと 長い なまえが 画面の 左に はみ出た）。
- たしかめ：`#boss #last #counterboss #weaklast #palbattle` を **800×1280（タブレット）**と 520×780・520×940。

## バトル画面の 整理（v12.9）

ユーザー「バトルシーンの アイテムが 大きすぎて 邪魔」「吹き出しで モンスター 隠れてるの 多い。特に タブレット」。**まず 測った**（harness `#bubble:<battle|boss|last>`＝ステージ px の 四角）：1体の てきは ふきだし（132〜284）と かさならないが、**2体 ならぶ とき（呼ばれた ザコ・ぶんしん・ダブル）は 左の 1体が ふきだしの うしろに 入る**（x 216〜250 から）。アイテムボタンは 143×48 で 右下の 1/4 を 占めて いた。

- **ふきだし＝アリーナの いちばん 下の 帯**（`.arena__msg`：left 8・right 62・bottom 6・13px・1〜2行）。勇者と てきの 足もと（bottom 62）の 下＝ゆかの 上 なので、だれも かくれない。**勇者と てきの あいだ（top 70・left 132）には もどさない。**
- **アイテムボタン＝44px の 四角**（`.bagbtn`：ふくろの 絵だけ・字なし・のこり数は 左下の 丸 `.bagbtn__n`・効果の 点は 右上 `.bagbtn__dots`）。
- **コンボと ためゲージは 勇者と てきの あいだ**（`.combo` top 62・left 134・24px／`.charge` top 64。コンボが 出て いる ときは `.combo + .charge` で 94・ひっさつつき（2行）なら 108）。
- **相棒の なまえと ゲージは 頭の 上**（`.pal__name` bottom 100%・`.pal__gauge` bottom calc(100% + 15px)・左ぞろえ）。足もとは 帯に なった ため。
- 塔の ふきだしの max-width（v12.8 の 136px）は 不要に なった ので 消した。
- たしかめ：`#battle #boss #last #summon #skill:clone #skill:call #elite #palbattle #atk:4 #counter #bagfx:burst #fx:fire:0.5 #palhit #firstplay:q1` を **800×1280** と 520×780・520×940。

## しゅぎょうば（v13.0）

相棒が となりで 声を かけながら 教える 画面（`#screen-dojo`・`js/ui/dojo.js`・CSS は `.dojo*`）。仕様は `docs/v13.0しゅぎょうばメモ.md`。

- **画面は 紺**（`--bg-1` → `--bg-0`）。上の 帯は HUD と 同じ `--hd-1/--hd-2`＋左に「‹」の もどる・ステージ名・右に **3だんの 点**（せつめい／いっしょに／ひとりで。おわった だんは みどり・いまは 金で 光る）。
- **左に 相棒 64px**（`MQ.enemies.node`・相棒が いなければ フクロン）、右に **白い ふきだし**（`.dojo__bubble`・しっぽは 左・字は 14px 太字・文が 変わる たびに `is-pop` で ふわっと）。ふきだしの 文は かならず「〇〇「…」」の 形（だれが 話して いるか 見える）。
- **例の カード・問題カードは バトルの `.card`（クリーム）を 借りる**。例の 中の だいじな 数は `<b>`＝オレンジ（`--org-2`）。問題文は まん中ぞろえ・字の 大きさは 文の 長さで `card__q--m / --s / --xs`。
- **「まず なにを する？」の 3たく**＝`.dojo__step`（クリームの 板・48px・左ぞろえ）。押した あとは 正しい ものが みどり・ちがった ものが 赤く なって **消えない**（見くらべられる）。
- **こたえの わく・テンキー・えらぶ**は バトルの `.display .keys .key .choice` を そのまま。**ヒントは 最初から 出す**（`.hintbox`）。
- **こたえの 表示**＝`.dojo__answer`（金の わく・28px の 金の 数字。正解の あとは みどりの わく）。
- 一覧は `.dojorow`（紺の 行・左に 教科の チップ `data-area`・右に ▶／✓）。**よしゅうの 行だけ 金の わく**。
- 合格は `.bagcard`（紺＋金わく＋金グラデの みだし）を 借りて 3つの ボタン（たたかいに いく＝みどり／しゅぎょうばへ＝クリーム／ちずへ＝紺）。
- **地図**：下の バーの いちばん 上に **青い「しゅぎょうば」**（`.dojobtn`・左に 相棒 30px）。修行で 開いた ステージには 金の ふだ「よしゅう」（`.node__pre`。**いまの ステージでは「いま ここ」と かさなる ので 右上に ななめ**）。霧の ノード（あと N こ）は つぎの ステージに 指導が あれば ボタンに なり、ふだ「よしゅう」＋「しゅぎょうばで」。
- **バーは どの 学年でも 出す**（v13.1）。中身が ない からと いって 消さない（「ないけど」と 言われた）。
- 自動の 指導の「れい」は `.dojo__exq`（問題を 22px で まん中）＋「こたえ → **答え**」＋ `.dojo__exnote`（答えの せつめい 14px）。
- 小1・小2で あそんで いる ときの 文は `MQ.dojo.KID`（小1の かん字だけ）。
- たしかめ：harness `#dojo #dojo:lesson #dojo:step #dojo:miss #dojo:done #dojomap #dojo:g2 #dojo:g5` を **800×1280 と 520×780**。
