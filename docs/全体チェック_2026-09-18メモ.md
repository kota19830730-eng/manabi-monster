# 全体チェック（2026-09-18・claude-5e）

ユーザー「全体のチェックをくまなくしてエラーやバグがないか確認お願いします。」

## やった こと
- 文法（node --check）ぜんぶ OK。smoke 23回 ALL OK（乱数の まれな 落ちも なし）。
- harness `#flow` エラー 0／`#fit:all` 800×1280 418回・520×780 416回 はみ出し 0。
- harness の モード 356本を 1つずつ 開いて ログの ERROR／MISSING を 集めた（scratchpad の sweep.js）。
- index.html・sw.js の FILES・harness の 登録もれ なし（boot.js が harness に ないのは わざと）。
- 本物の index.html と 公開中の サイトを 開いて コンソールの エラー なし。
- 3分野（セーブの 引きつぎ／v14.7〜v14.10／バトルの 流れ）を コードで 読んで、見つけた ものは ぜんぶ 自分で 再現・確認した。

## 直した バグ（14件・まだ 公開して いない）
| # | どこ | 何が おきて いたか | 直し |
|---|---|---|---|
| 1 | `js/ui/battle.js` sayAnswer | 2回 まちがえた あとの「こたえは ○○。」に 学年の 辞書が かかって いた。小2いじょうが 小1の「『足』の よみかたは？」で 答えが「足」と 出た（公開中の 版で 再現）。小1では かん字の 答えが ひらがなに なった | 答えの ぶんを raw の 部品に |
| 2 | `js/ui/battle.js` 書き取りの ×の ふきだし | 「「山」は 3画（かく）」が 小1で「「やま」は 3かく（かく）」、小3で「3画（書く）」 | 字は raw・「画」だけに |
| 3 | `js/ui/dojo.js` しゅぎょうば | 1と 同じ（答え・せつめいに 辞書が かかる。答えの わくも raw では なかった） | `ansSay()` で raw |
| 4 | `js/core/pals.js` evolveIfReady | 進化先（2段階め）を もう 持って いると **上書きして 1体 消えて いた**（なまえも） | 1体に まとめる（けいけんちは 多い ほう・なまえは のこす） |
| 5 | `js/ui/photo.js` save | 写真の モンスター 41体めで いちばん 古い 子が だまって 消えた | 40体で とめて「いらない 子を 消してね」 |
| 6 | `js/ui/battle.js` applyRewards | 塔で にげた 敵を とっくんで たおしても 消えず、ずっと もどって きた（けいけんち ＋15 を 何度でも） | 消す 先に 'tower' も |
| 7 | `js/ui/battle.js` applyRewards | ボスの 問題を 2回めで 正解 → つぎの たたかいで **ボスが「もういちど」の ザコとして 出て、たおすと ボスを たおした あつかい**（しょうごう） | にげた敵と 同じく ザコの すがたに |
| 8 | `js/core/battle.js` まちがいの ところ | 「すきだらけ！」の 問題で まちがえると 予告なしで ガードくだき（本気では たてが こわれた） | bossOpen を 消す 前に guardOnMiss |
| 9 | `js/core/letter.js` read | 同じ日の 2通めの てがみが 1通めの ミッションを つかい回し、ごほうびが 出なかった | てがみの ミッションは 入れかえ |
| 10 | `js/core/missions.js` ensure | 夜に 読んだ てがみの ミッションが 日づけで 消えて ごほうびが 出なかった | 読んだ まま できて いない ぶんは あしたにも のこす（`MQ.letter.carryMission`） |
| 11 | `js/core/capsule.js` pick | シークレット 3体を そろえた あと、まだの げきレアが あるのに 25%で かぶりが 出た（天井でも） | えらんだ がわに まだが なければ もう いっぽうへ |
| 12 | `js/ui/result.js` | けっか画面から 1.8秒 いないに つぎへ 行くと バトルの 曲が 地図の 曲に 上書き | けっか画面の ままの ときだけ |
| 13 | `js/ui/battle.js` ボスへの こうげき | ビッグバン（1.9秒）・スターバースト（2.5秒）が ボスでは 1.7秒で 切れた | ザコと 同じく 見おわるまで まつ |
| 14 | `js/core/battle.js` summary | ごちゃまぜ・とっくんで にげた 敵の ステージが 'mix3' など に なって いた | 問題の もとの ステージ |
| ＋ | `js/core/save.js` persist | 容量が いっぱいでも だまって いた | 1回だけ こまったとき 記録＋トースト |

テストの 古い 行も 2つ 直した（`tools/harness.html` の `#cont`＝小6は もう あそべる／`#aiparent`＝せっていは おうちの人ページ）。

## ギンガの たてに 上限（2026-09-19・ユーザー「1はOK」）
- 前は たてが へらず「ずっと まもる」＝解けない 数の 問題で 答えが 出ずに くり返し、いつも ★3・マントの ＋5コイン。
- **1問 3回まで まもる**（`js/core/battle.js` の `GINGA_SAVES`・`s.gingaQ/gingaN`）。4回めは ふつうに にげて 答えが 出る。説明文は「たてが へらずに 1もん 3かいまで まもって くれる」（hero.js）。smoke に 4回めの 検査。
- 公開は sw 175（お知らせ なし）。

## 直して いない
- 目玉モンスターに 子どもが「目 2つ」を えらぶと 2つ目に なる（見た目だけ）。
- 明るい 写真でも えんぴつの 線が なく 色だけの 絵だと「くらい しゃしん」と 出る かも（未確認）。

## テストが 古い だけ（アプリは 正常）
`#counterboss`（v12.7 で ボスが 強く なり、ためが 来る 前に たおして しまう）・`#skill:clone2` の banner1・`#news` の「つぎ」（前から）・`#guard`（わざと エラー）・`#kanso`（にせの 記録）。
