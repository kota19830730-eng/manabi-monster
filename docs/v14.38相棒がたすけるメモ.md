# v14.38 相棒が たすける（C）

v14.37（相棒の ターン）の 壁打ちで 出した C「相棒を ピンチを たすける 役にも」。
ユーザー「公開して。それが おわれば C を やりましょう」（2026-09-26）。

## 1. ねらい
- A＋B で 相棒の「こうげきの 見せ場」は できた。C は **こまった ときに たよれる 相棒**。
- v14.37 の 数え（palcount.js）で、正答 60% の 子は 相棒だけの 瞬間が 4.7回＝**にがてな 子ほど 相棒に 助けられる 場面が ある**。

## 2. できた もの
| | 中身 |
|---|---|
| **ささやき（ヒント）** | わざが たまって いない ときに 相棒を タッチ → いまの 問題の ヒント。中身は みちしるべと 同じ（ヒントの 文／えらぶ問題は まちがいを 1つ 消す／ローマ字は さいしょの 2字）＝**答えは 見せない**。1たたかい **1回（♥2 で 2回）**。たからばこ・まちがえ直し（もう ヒントが 出て いる）・もう ヒントを 見た 問題では 出ない。タイムアタックでは なし |
| **かばう** | まちがえた とき コンボを まもる（v14.36 は ♥3 だけ）→ **だれでも 1回（♥3 で 2回）**。相棒が 主人公と てきの あいだに とびこんで 光の たて（`.pal__shield`） |
| 知って もらう | たたかいの はじめの ひとことが 半分の 見こみで「こまったら ぼくを タッチ！ ヒントを おしえるよ」。メニューの 相棒カードに「たすける：ヒント N回・かばう N回」 |
| きずな | ♥2「なまえを よんで くれる・ヒントが 2回」／♥3「かばって くれるのが 2回に」 |

タッチの じゅんばん：わざが たまって いる → かまえ（v14.37）／かまえ中 → ひとこと／それ いがい → **ささやき**／ささやきが 使えない → わけを ひとこと（もう 出て いる・つかった・おいうち あと N回）。

## 3. ルール
- 大原則は そのまま：答えを 見せない・ばつなし・押さなくても 何も へらない。
- ボスの 強さ（v12.7）：ささやきは ボスの 問題でも 使える（みちしるべ アイテムと 同じ）。1たたかい 1〜2回 なので 勝率への 影響は 小さい。

## 4. ファイル
- `js/core/pals.js`：`moveOf` に `whisper`（1／♥2 で 2）・`cover`（1／♥3 で 2）・BOND_PERKS の 文
- `js/core/battle.js`：`s.palWhisper`／`palWhispered`・`palWhisperInfo()`・`palWhisperNow()`（`MQ.battle.palWhisper`）・summary に `palWhispered`
- `js/ui/battle.js`：`palTap` の さいごに `palWhisperTap()`・`palCoverFx()`・はじめの 案内・かばった ときは はげましの ひとことを 出さない
- `js/core/sfx.js` `palWhisper`・`css/style.css` いちばん 下「v14.38 C」・`js/ui/dex.js` `.palnow__help`
- `js/content/news.js` v13.51（sw 213）・`sw.js` 213・smoke「相棒が たすける（v14.38）」・harness `#palhint[:cover]`

## 5. たしかめ方
- `node tools/smoke.js .`（ALL OK×3）
- harness `#palhint`（相棒を click → ヒント・のこり 0）`#palhint:cover`（2問 正解 → まちがえ → かばう）。**サポート（ヒント先出し）と よろいの 時とめ が 先に 効く ので、harness では 切って いる**
- 画像 デスクトップ `相棒のターン/10_ヒント` `11_かばう`

## 6. もどさない こと
- ささやきで 答えを 見せない（makeHint の max 1）
- かまえ（わざ）が たまって いる ときは タッチ＝かまえ（ささやきより 先）
- かばった ときに はげましの ひとことで「まかせて！」を 上書きしない

## 7. のこり
- **公開ずみ（2026-09-26・sw 213・4a3c55b・news v13.51）**
- 実機 未確認（タッチで ヒントに 気づけるか・ヒントが 1回で たりるか）
