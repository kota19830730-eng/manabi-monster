# v3.2 写真から ドット絵（2026-08-31）

息子さんの 本物の 絵（夜の 室内で とった 4コマの 紙・えんぴつ＋色えんぴつ）を、ゲームの
「じぶんの モンスターを つくる」に そのまま 通したら **ほぼ 何も 出なかった**（点が ぱらぱら。
自動わくは キーボードを えらぶ）。ユーザー「子どもの 意図を 線が 細かろうが 再現しないと いけない」
→ `js/ui/photo.js` の「しらべる 道具」を 作り直した。写真は `写真ドット絵テスト/`（作業場・git の 外）。

## なぜ 出なかったか
1. 「はいけいを 消す」の しきい値が **固定**（明るさの 差 90）→ 暗い 写真の えんぴつ線は 紙と 見なされる
2. 自動わくは「紙でない いちばん 大きな かたまり」→ キーボード
3. 紙の 明るさは 見ていたが **色**は 見ていない → 黄色い 電灯で 紙が 黄色く、色えんぴつが くすむ
4. 1まいの 紙に 4体 → 正方形の わくに 題名・仕切りの 線が 入る

## 作り直した しくみ（photo.js）
| 段階 | 前 | いま |
|---|---|---|
| 紙の 色 | 8×8 の ますごとに 明るさ 1本 | 8×8 の ますごとに **R・G・B**（`paperMap`）。候補は **色みの ない 点**だけ（色えんぴつの 塗りで うまった ますは まわりに 合わせる） |
| 正規化 | なし | **写真を 紙の 色で わって「白い 紙に かいた 色」に**（`normalize`）。暗い 写真・黄色い 電灯・かげ でも 同じ 結果 |
| しきい値 | 固定（スライダー） | **写真ごとに 自動**（`thresholds`：紙らしい 点の 中央値＋ゆらぎ×3。2段階で しぼる）。スライダー「はいけいを 消す」は その 倍率（55＝自動の まま） |
| 紙でない 点 | 明るさ＋色み | 「紙より 暗い」or「色が ある」（`notPaperMask`）＋ ごみ消し ＋ **端から 端まで とどく まっすぐな 線を 消す**（`removeLines`：途切れない・細い・えんぴつ色 の 3条件。仕切り線・罫線。本の へり など 端まで とどかない 直線は 消えない） |
| 絵の 中 | 外から つながる 紙を 背景に | 同じ。ただし **線の すきまを 半径 2 で うめて 見る**（`dilate`）ので、線で 囲まれた 白（ガイコツの 顔）は 絵の 中 |
| 本体だけ | なし | **いちばん 大きな かたまり＋近く（5%）**だけ（`keepMain`）。ふちに かかる 細長い もの・小さな 切れ端・10px 未満の ごみは 外す |
| マスの 色 | 暗ければ 線 | 1マス 36点を **えんぴつ（暗くて ほんとうに 灰色）／色／白っぽい**に 分ける。えんぴつ 3点 いじょう → 黒い 線（うすい 線ほど 少し 明るい 灰色）。ただし 平均に 色が あれば（黄色い 電灯の 下の 水色えんぴつ）その 色の 線。色 3点 いじょう → その 色（`dense`＝白からの 距離 1.7倍 で 色えんぴつの うすさを こく、`vivid`）。細い 色の 線は 少し こく |
| わく | 正方形だけ | **長方形で OK**（右下の つまみで たて・よこ べつべつ）。できあがりは 絵の はんいで 正方形に する。`workCanvas` は 比を たもつ |
| 自動わく | 大きな かたまり | 写真ぜんたいを 正規化 → 紙でない かたまり → **ふちに かかる もの・長くて 細い もの を 外して いちばん 大きな 絵**＋近くの 小さな もの（3%）。となりの 絵は まぜない |
| バトル | 72px | **じぶんの 絵の モンスターは 96px**（`battle.js`・64マスが つぶれない）。写真画面の「バトルでは」も 96px |

ドラッグ中の 作り直しは 1フレーム 1回（`scheduleRefresh`）、指を はなした ときは かならず 作り直す。

## 結果（息子さんの 4体・スライダー初期値の まま）
| | 前 | いま |
|---|---|---|
| スカルホース | 点 51マス | 白い ガイコツ＋赤い 目・黒い 体・オレンジの 馬・青い 水 |
| サメオニ | 点 60マス | 形・目 3つ・歯。水色は 青灰色（黄色い 電灯で 灰色に 写るため） |
| ABC3きょうだい | 点 194マス | 赤A・緑B・黄C、目も 出る（いちばん きれい） |
| ずかんの あくま | 点 62マス | 本＋目＋羽＋剣。長方形の わくに すれば 題名は 入らない |
自動わく：1体だけの 写真 → その 絵。写真ぜんたい（4コマ）→ スカル＋ABC が 1つに なる（絵が 仕切り線に 触れている ため。わくを 指で 合わせれば OK）。

## のこる 課題
- **自動わく**は 1体だけの 写真なら その 絵を えらぶが、この 4コマの 紙のように 絵と 題名が くっついている・絵が 仕切りの 線に 触れている ときは 題名や となりの 絵まで 入る ことが ある（写真ぜんたい → スカル＋ABC）。**わくを 指で 合わせる（長方形 OK）のが 確実**。本体の 上／下に まるごと ある 小さな かたまりは 題名と 見なして 外す ルールは 入れてある（`cluster` の vtol）
- 4コマの 紙で 題名が 絵に くっついている とき（ずかん）は 題名が 入る → 長方形の わくで 外す か「ドットを 直す」の 消しゴム
- 黄色い 電灯の 下の 水色えんぴつは 青灰色に なる（紙の 色で 割っても 色みが 少ない）
- 色えんぴつの 塗りが うすい ところは 白が まざる（子どもの 塗りの まま）
- 実機（タブレット）の 速さは 未確認（320px で 線さがし 17角度×2 ＋ 正規化。PC では 一瞬）

## v3.3 じどうで きれいに（自動補正・2026-09-01）
ユーザー「粗い。細かい ドット絵に 訂正できる 自動補正を」「子どもの ぬりむらを 一色に 統一できたら いいね」→
- **初期の こまかさを 96マス**（こまかい）に（前は 64）。
- **「じどうで きれいに」**（`cleanCells`・初期 ON・する／しない の チップ）：
  ①ぽつんと ある マスを 消す ②線の 1マスの すきまを つなぐ ③6マス いじょう かこまれた あなを うめる（多数決）×2
  ④ぬりの 中の 白い ぬけを まわりの 色に ×2 **⑤ぬりむらを 一色に**（色あいが 45°以内で つながった マスを ひとまとまりに して、
  こく ぬれている 半分の 平均色に そろえる）⑥ふちの マスを 55% に こく して 輪かくの 線に（ゲームの スプライトらしく）。
  マスを うごかさない・ならべ直さない（子どもの 絵の 形は そのまま）。
- テスト：`#photo3` の スライダー引数が `消す,こく[,きれい(1/0)]` に。`info().clean`。sw v47。

## v3.4 ゲームふう（2026-09-01）
ユーザー「子どもの イラストを 尊重し過ぎ。もっと ゲームっぽく」→ しあげを 3だんかいに：
**ゲームふう（初期値）／きれいに／しない**（チップ・`cleanMode` 2/1/0・`setOptions({clean})`）。
ゲームふう（`gameStyle`）＝ きれいに の あとに ①線の マスを ぜんぶ 同じ こい 色 [26,24,38] に（輪かく 1本）
②色あいは 子どもの ままで HSL の S×1.4（0.55〜0.92）・L 0.42〜0.66 に そろえる（ゲームの スプライトの こさ）
③左上に ハイライト（白 30%）・右下に かげ（×0.72）＝ blocks.js と 同じ 立体感。
**白の 見分けは chroma（RGB の 差）で**（HSL の s は 白に 近いほど 大きく 出て ガイコツの 顔が 赤く なった＝はまりどころ）。
chroma<0.13 かつ L>0.72 → 白の まま／chroma<0.09 → あざやかに しない。ゲームふうは 16色 そろえ（nearest）を 通さない。sw v48。

## v3.5 ゲームふうを「ほかの モンスターと 同じ」に（2026-09-01）
ユーザー「まだ 尊重し過ぎ。**ほかの モンスターと 比べて 違和感の ない ように**」→ ゲームの てき（`monsterart.js`・48×48・
5〜12個の 四角・色 3〜5色・黒ふち なし・右下 暗い面＋左上 ハイライト）に 合わせた。`gameStyle` の 中身：
1. **48マス 固定**（バトルの 96px 表示で 1ドット＝2px＝もとの てきと まったく 同じ 大きさ）。こまかさの チップは かくす
2. `unoutline`：**絵の ふちの 黒い 線を 体の 色に**（ゲームに 黒ふちは ない。STYLE_GUIDE）。中の 線（目・口）は のこす
3. `hatchToShade`：**中の 細い えんぴつ線（かげぬり）を 体の 色の こい 面に**（5×5 の 半分 いじょうが 線＝ぬりつぶし は そのまま）
4. `inkThin`：同じ 色の ぬりに まぎれた 線を ぬりに
5. `solidify`：すき間を 閉じて つまった 体に（closing 半径2）
6. `dropIslands`：いちばん 大きな かたまりの 8% 未満を 消す（題名の 字・となりの らくがき）
7. `fewColors(5)`＋`smooth`×3＋`mergeSpecks`（5→8マス）：**色 5色の べた塗り**・点々や ぎざぎざを 消す
8. 立体感：下と 右を ×0.6、上と 左に 白 35%（`blocks.js` と 同じ）
**たしかめ方**：`scratchpad/photo/side.html`（ゲームの てき 3体＋自作 4体を 96px で ならべる。enemies.js を 直に 読む）。
**はまりどころ**：`sizeRow` を `sizeChips` より 前に 宣言して ReferenceError（#flow が 検出）。宣言の 順に 注意。sw v49。

## v3.6 絵から 特徴だけ 読んで 組み立てる（2026-09-01・いまの 本命）
ユーザー「全然 ちがうなぁー。**子どもの 絵は 参考にする 程度で 普通に もともと いる モンスター風に** したら おかしく なくなる かな？」→ そのとおり。
写真を 1マスずつ ドットに する かぎり、もとの てき（数個の 四角で できた 単純な 形）とは 質感が ちがう。
**`js/content/monstergen.js`（新）**：
1. `analyze(cells, N)` … 絵から **色**（ひろさ ×あざやかさ で えらぶ。紙の 白・まっ黒は 点数を 下げる）・**たてよこ**・
   **上の でこぼこ＝つの**・**下の でこぼこ＝あし**・**こい 小さな かたまり＝目の 数**・**ゆびもん seed** を 読む
2. `make(f)` … けもの型／さかな型／立ち型／まる型 の 4つから えらび（たてよこ に 合う ものの 中から seed で 決める＝
   同じ 絵なら いつも 同じ・ちがう 絵なら ちがう）、monsterart.js と 同じ 書き方の 四角の ならびを 返す
3. `png(shape, colors)` … blocks.js と **同じ ぬり方**（右 -30%・下 黒15%・左上 白50% の ハイライト・A から B/C/D を 自動）で 48×48 の PNG
しあげの チップは **モンスター（初期値）／ゲームふう／絵の まま** の 3つ（`cleanMode` 3/2/1）。
できあがりは PNG なので、ずかん・バトル・ドット絵エディタは そのまま つかえる。
**たしかめ方**：`scratchpad/photo/side.html` で もとの てき（スライム・スカル・リザード）と 96px で ならべる。
smoke に monstergen の 検査（特徴・4しゅるい・48マスに おさまる・同じ 絵なら 同じ）を 追加。sw v50。

## v3.8 部品ライブラリ＋3体の 候補から えらぶ（2026-09-01・いまの 本命）
ユーザー「全部 ただの 犬やん。特徴 捉えてなさすぎ」→ 型が 4つ しか なく、しかも 3体が 同じ 型に なって いた。
**わかった こと**：子どもの えんぴつの 絵から「サメ／馬／文字」を 見わけるのは 無理（4まいとも ratio 1.39〜1.51・parts 1・legs 0〜2 と ほぼ 同じ 数値に なる）。
そこで **①部品を ちゃんと ふやす ②1つに 決めうち せず 3体の 候補を 出して 子どもに えらんで もらう**。
- 体 8しゅるい：まる／けもの／さかな／とり／人がた／むし／はこ／3つご（）
- おまけ：ガイコツ頭（白と 黒が はっきりした 絵の ときだけ）／つの／はね／しっぽ／きば／目 1〜3つ（）
- 特徴は （いくつに 分かれて いるか）・（四角さ。**だ円でも 0.79 に なる ので 四角は 0.9 いじょう**）・（よこの 出っぱり）・・・・つの・あし・目の数・色
-  が しゅるいごとに 点を つけ、上位 3つを  が 作る。同点は 絵の ゆびもんで 順が 変わる
- 画面：できあがりの 下に「どれに する？」の タイル 3つ（・ の ）。えらんだ ものが なかまに なる
sw v51。**はまりどころ**： を つかう 場所より 後で 宣言して ReferenceError（#flow が 検出）。宣言の 順に 注意（2回目）。

## テスト
- `tools/harness.html#photo3:<encodeURIComponent した file URL>[:<マス数>[:<x,y,w[,h]>[:<消す,こく>[:down]]]]`
  x,y,w,h は 写真に 対する わりあい（PointerEvent で わくを 動かす＝子どもの 指と 同じ）。`down` で できあがりまで スクロール。
  ログに `png:`（できあがり）`cut:`（わくの 中）`norm:`（白い 紙に 直した）`cls:`（判定の 図）を 出す。
- `MQ.ui.photo.debug()`（テスト用）：いまの わくの 正規化画像と 判定図。
- `#photo2`（にせの 絵）は 前と 同じに 変換できる（回帰なし）。
