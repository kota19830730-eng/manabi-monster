# v13.21 ABC3きょうだいを 1体に（2026-09-14・claude-f7）

## きっかけ
ユーザー「ABC兄弟の クオリティが 低く 見えるから ゲーム内も 作りなおして。Aだけですね。3兄弟じゃない。**一つの モンスターで 3体 ひっついてる**ので 作成しましょう」。
聞いた こと 3つ（名前は そのまま／進化形は 1本の 3段階に／トリプルKO は なし）→ ユーザー「すべてOK！」。

## 息子さんの 絵（紹介資料/作り方/img/drawings.jpg の 左下）
- 赤い A・緑の B・黄色い C が **かさなって くっついた 1体**。
- A の てっぺんと B の 上に **丸い 目玉**（えんぴつの 黒い ふち）。字の 線の あちこちに 目（まるの 中に 点）。
- A の よこぼうは **ギザギザの 歯の 口**。字の 下に 足（A は 字の 足が そのまま 足）。C は 右下が しっぽの ように のびる。

## できた もの
| id | 名前 | 絵 | 出方 |
|---|---|---|---|
| `abc` | ABC3きょうだい | `abc`（tools/son/abc-base.js） | 英語の空の レア敵（ほかの 息子さんの モンスターと 同じ） |
| `abc-2` | ABCナイツ | `abc2`＝かぶと 3つ＋A の けん＋金の むねあて | 相棒 Lv10（evoOnly） |
| `abc-3` | ABCロード | `abc3`＝金の かんむり 3つ＋A の しゃく＋光の つぶ | 相棒 Lv20（evoOnly） |

- 色：A＝赤（`A`）、B＝緑（`G`）、C＝黄色（`Y`）。`blocks.js` の `MAT_BY_KEY` に G・Y を 体の 材質として 足した。
- 頭の 目玉の 黒い ふちは 色キーで なく `'#141018'`（`k` に すると まばたきの 候補が 4つを こえて まばたき しない）。smoke が「まばたき する」を 検査。
- 重ねる じゅんは C → B → A（A が 前）。B の あなは すかして 中に 目。C の 右は あける。
- 進化形は 48マス いっぱいなので **`lower: 3`**（足を 3マス みじかく して 上を あける）。`fitTop`（小さく する）は 4マスの 線が 3マスに つぶれる ので つかわない。
- 図かん 275 → **269体**。

## むかしの もの（消した）
- 3体 `abc-a` エー／`abc-b` ビー／`abc-c` シー（`trio: 'abc'`・英語の空で 35% の 確率で 3体同時＝トリプルKO）。
- 進化形 6体（エーナイト／エーロード／ビーナイト／ビーロード／シーナイト／シーロード）。
- 絵 `letterA/B/C`・`letterA2`〜`letterC3`。
- **3体同時の しくみ（core の `trioIds`・`XP.tripleKO`・ui の `TRIO_CHANCE`）は のこした**。`MQ.enemies.trioFor()` が いつも null なので 出ない。

## 古い セーブの 引きつぎ（`js/core/save.js` の `mergeAbc`・migratePlayer が よぶ）
- 図かん：たおした 数は いちばん 多い もの（3体同時で 3つとも ふえて いた ので 足さない）。NEW も うつす。
- 相棒：**消さない**。同じ すがたに なる ものは いちばん 育って いる 1体を のこす。つけた なまえ・さいしょの 日も のこす。連れて 歩いて いたら 新しい id で。
- にげた敵・ふくしゅうの `enemyId` も おきかえる（ない id だと 絵が 出ない）。
- 何回 よんでも 同じ（smoke が 検査）。

## 作り方（絵を 直す とき）
```
tools/son/abc-base.js を 直す（1段階め）／ tools/son/defs.js の abc の 節を 直す（進化形の かざり）
node tools/son/build.js      … tools/son/sheet.html で 見る
node tools/son/abc.js        … monsterart.js と enemies.js の <abc> <abc-evo> の しるしの あいだを 書きかえる（何回 流しても 同じ）
```
**monsterart.js / enemies.js の しるしの あいだを 手で さわらない。**

## たしかめ
- smoke：ABC の 節（9体が いない・3色・目 10こ いじょう・まばたき・引きつぎ・Lv10/Lv20）・図かん 269。
- harness `#sonevo`（4系統 × 3段階・3体同時の 組 なし）`#sonevo:art`・`#eigo4:1`（|js= で 1体めを abc に して 3D を 見た）・`#news2`。
- 確認ページ https://claude.ai/code/artifact/c8ee5308-59bc-4f76-b7cc-42072c2c0d5b ・画像 デスクトップ `ABC3きょうだいを1体に/`。

## もどさない
- もとの 絵の かたち（くっついた 3文字・目 たくさん・歯・足）を くずさない（v2.4.1「再現性が 低い」）。
- べつべつの 3体に もどさない。
